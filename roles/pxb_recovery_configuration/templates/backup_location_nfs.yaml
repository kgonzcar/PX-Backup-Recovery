apiVersion: stork.libopenstorage.org/v1alpha1
kind: BackupLocation
metadata:
  name: "{{ pxb_recovery_configuration_backup_location_name }}"
  namespace: "{{ pxb_recovery_configuration_namespace }}"
location:
  type: nfs
  path: "{{ pxb_recovery_configuration_backup_location_nfs.path }}"       # subdir under export, or "/"
  sync: true
  nfsConfig:
    server: "{{ pxb_recovery_configuration_backup_location_nfs.server }}"
    exportPath: "{{ pxb_recovery_configuration_backup_location_nfs.export_path }}"
